{
  "name": "@mapflux/geocoder",
  "version": "0.1.0",
  "description": "Geocoding abstraction for MapFlux — Nominatim + Photon",
  "license": "MIT",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "import": "./dist/index.mjs", "require": "./dist/index.js", "types": "./dist/index.d.ts" } },
  "files": ["dist"],
  "scripts": { "build": "tsup src/index.ts --format cjs,esm --dts --clean", "test": "vitest run", "clean": "rm -rf dist" },
  "devDependencies": { "tsup": "^8.0.0", "typescript": "^5.5.0", "vitest": "^2.0.0" }
}
